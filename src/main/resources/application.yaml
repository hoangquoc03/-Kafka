spring:
  application:
    name: order-service
    datasource:
      url: jdbc:postgresql://localhost:5432/orderdb
      username: postgres
      password: 123456

      jpa:
        hibernate:
          ddl-auto: update

      kafka:
        bootstrap-servers: localhost:9092
